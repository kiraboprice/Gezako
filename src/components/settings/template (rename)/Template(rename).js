import React from "react";

import "./template.css";

const Template = () => {
    return (
        <div id="reports-section">
            This template will be used for each setting once all the details have been 
            mapped out. Untill then we will have this template.
            <br />
            Since all the settings will not be the same, the contents of this template will
            have to be copy pasted to other similar setting component. Unless there is a way
            all the components will be similar then we can just reuse this one template.
        </div>
    )
}

export default Template;
